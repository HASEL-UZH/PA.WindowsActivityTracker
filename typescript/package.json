{
  "name": "windows-activity-tracker",
  "version": "2.1.0",
  "description": "A cross-platform windows tracker that maps window titles to activities",
  "author": {
    "name": "HASEL",
    "email": "dev@hasel.dev"
  },
  "type": "module",
  "exports": "./dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --config jest.config.json",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "get-windows": "^9.3.0"
  }
}
